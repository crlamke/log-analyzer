# log-analyzer - A log analyzer written in Python

This tool will analyze a text based logfile with a format defined in the log_analyzer.cfg file. It supports timestamp-based filtering and exports nicely formatted results to an Excel doc for viewing and further analysis.
